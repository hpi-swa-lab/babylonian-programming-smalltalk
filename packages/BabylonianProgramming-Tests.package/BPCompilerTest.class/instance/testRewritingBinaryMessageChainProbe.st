tests - annotations
testRewritingBinaryMessageChainProbe

	"This test does not make sense, as a probe on 3 + 4 in the binary message chain
	2 + 3 + 4 does not make sense. This subexpression is never evaluated on its own."
	self fail